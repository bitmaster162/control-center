# Project Policy

- Human Sovereign: Robert.
- This module is shadow-only: it may observe, diagnose, propose and test candidate deltas; it may never self-apply them.
- External model APIs and network calls: DENY.
- Source/project repositories: READ-ONLY.
- Writes are limited to this module's own state/output roots.
- No raw secret values in logs; metadata/fingerprint only.
- No claims of sentience, intrinsic cross-session memory or background model existence.
- Recursive depth is bounded; no material delta means STOP.
- Human approval is required before promotion of any CORE/BRAIN/policy change.
- `can_trade=false`; capital permission `DENY`.
- Archive completeness claims must be scope-bound by scope ID, manifest hash, numerator/denominator and evidence ceiling.
- Archive advancement requires origin, material correction/pivot and current physical state; selection alone does not prove causality.
- Primary Source-001 and legacy analytical cursor identities must remain separate.
- Historical liveness requires fresh target readback before current promotion.
- Probe counts, evidence-family counts and root-cause counts are separate.
- A filesystem root is not a Git repository root without exact git-top-level verification.

- Every quantitative claim must bind to one count universe and explicit overlap/disjoint semantics.
- Occurrence count and independent evidence-family count must remain separate.
- A coherent canon or research report is not current implementation without physical receipts.
- Multiple mutable authority surfaces without one verified effect broker force HOLD.
- A proof ledger without proof identity fields is narrative evidence debt.
