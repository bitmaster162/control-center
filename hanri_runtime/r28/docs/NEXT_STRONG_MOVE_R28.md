# Next Strong Move R28

## No new agents and no reruns

1. Install or run R23 Return Sync to discover already-completed outputs.
2. Intake the existing Hermes Wave-013 package by exact prefix/hash.
3. Do not advance Source-001 beyond rank 260 until those physical bytes pass integrity and claim adjudication.
4. Quarantine the raw handoff recovery ZIP from general distribution; use R28 ledgers and sanitized derivatives.
5. Replace collector V1 only through a bounded collector V2 patch with a verified Git baseline and regression tests.

## Collector V2 acceptance gates

- zero copied recovery-self-derivatives counted as primary;
- zero secret literals after second-pass payload scan;
- separate source-effect/content-secrecy/integrity/readback receipts;
- reason-accurate unsafe counts;
- file-level artifact-role and derivation ledger;
- source and destination hashes equal;
- no source modification;
- `can_trade=false`.

## Archive continuation

After Wave 013 physical acceptance:

```text
Source-001 241–260 accepted
→ open exact ranks 261–280
→ one causal block receipt
→ one current-state delta
```

The next pass is not another global report. It is one physically admitted chronological block plus its provenance crosswalk.
