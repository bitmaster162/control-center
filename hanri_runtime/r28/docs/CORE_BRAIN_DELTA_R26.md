# CORE / BRAIN Candidate Delta R26

Status: `CANDIDATE / PROMPT-LEVEL ONLY`

## CORE additions

### 1. Causal-spine requirement

Archive promotion must preserve the material correction path:

```text
origin → correction/supersession → current physical state
```

### 2. Completeness scope

No global completeness language without a manifest-bound global scope.

### 3. Liveness freshness

Historical liveness is an event, not a current status. Current liveness requires a fresh target readback.

### 4. Probe/root-cause separation

Track separately:

```text
probe_count
failing_probe_count
evidence_family_count
root_cause_count
```

### 5. Repository identity

A filesystem root does not inherit Git authority from nested repositories.

### 6. Multi-cursor archive

Every archive lineage carries its own source hash, denominator, cursor and evidence ceiling.

## BRAIN additions

New reflex:

```text
DO NOT ASK ONLY “WHERE DID WE START?”
DO NOT ASK ONLY “WHERE ARE WE NOW?”
ASK “WHICH CORRECTION CHANGED THE PATH?”
```

Human-native view shows intent, correction and present consequence. AI-native state preserves exact source/hash/time/cursor/scope.
